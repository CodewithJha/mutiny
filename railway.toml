# Mutiny Hosted API — Railway deploy from monorepo root
[build]
builder = "nixpacks"

[deploy]
startCommand = "uv sync && mkdir -p data && uv run uvicorn mutiny_api.main:app --host 0.0.0.0 --port ${PORT:-8000}"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
