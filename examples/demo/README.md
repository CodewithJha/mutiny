# Backup demo artifacts

When live campaign search fails at Iris, use this path — never invent DB violations.

## Automated fixture path (preferred nuclear option)

```bash
PYTHONPATH=apps/api/src:apps/demo_agent/src:packages/mutiny_core/src \
  uv run python scripts/backup_fixture_demo.py
```

Walks:

1. Oracle on [`../traces/m4_refund_limit_violation.json`](../traces/m4_refund_limit_violation.json)  
2. Regression FAIL on soft agent via [`../regressions/refund_limit_m5.json`](../regressions/refund_limit_m5.json)  
3. Same artifact PASS after `DemoSupportAgent(enforce_refund_policy=True)`  

## Screen recording (optional)

Place a real capture at `examples/demo/backup.mp4` before the event if you want video fallback (DEMO_SCRIPT §4). Until then, the fixture path above is the checked-in backup.

## Last green smoke transcript

Generated by:

```bash
PYTHONPATH=apps/api/src:apps/demo_agent/src:packages/mutiny_core/src \
  uv run python scripts/smoke_reliability.py | tee examples/demo/last_green_smoke.txt
```
